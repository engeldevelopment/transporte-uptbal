name: Integración continua

on:
  - push
  - pull_request

jobs:
  unittest:
    name: Pruebas unitarias
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
       
      - name: SetUp python
        uses: actions/setup-python@v2
        with:
          python-version: 3.7
    
      - name: Instalación de dependencias
        run: pip install -r requirements/dev.txt
    
      - name: Ejecución de pruebas unitarias y covertura
        run: make coverage

      - name: Verificando calidad de código
        run: make check_qc

      - name: Generar reporte de covertura
        run: make coverage_report
      
      - name: Agregando action para subir el artefacto
        uses: actions/upload-artifact@v2
        with:
          name: reporte-de-coverage
          path: htmlcov/
